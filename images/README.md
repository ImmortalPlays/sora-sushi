# Images

Drop your real photos in this folder, then swap the placeholder tiles in
`index.html` / `styles.css` for `<img>` tags pointing here.

Suggested shots:
- `about.jpg` — chef at the counter
- `gallery-1.jpg` … `gallery-6.jpg` — food and the dining room

Tip: keep images under ~400 KB each so the site loads fast.
